apiVersion: v1
kind: Service
metadata:
  name: spring-cloud-gateway
  namespace: microservices-namespace
spec:
  type: LoadBalancer
  selector:
    app: spring-cloud-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
